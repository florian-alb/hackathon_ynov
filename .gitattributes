*.json filter=lfs diff=lfs merge=lfs -text
*.safetensors filter=lfs diff=lfs merge=lfs -text

# Rendu CYBER : petits fichiers texte, à garder en git normal (pas en LFS)
rendu/cyber/*.json !filter !diff !merge text
